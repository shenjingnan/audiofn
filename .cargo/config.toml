# macOS 最低部署目标：与 src-tauri/tauri.conf.json 的 bundle.macOS.minimumSystemVersion
# （13.7.8）保持一致。rustc 对 apple-darwin target 自带的默认部署目标低于应用声明值，
# 而 cc/cmake 构建的 C/C++ 依赖会读这个环境变量；显式对齐可保证整条构建链的
# 最低系统版本一致（该变量在 Linux 上无意义，被忽略）。
[env]
MACOSX_DEPLOYMENT_TARGET = "13.7.8"
